{
  "name": "openedge-dsl-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "docs:examples:generate": "npx tsx scripts/generate-example-artifacts.ts",
    "docs:artifacts:generate": "npx tsx scripts/generate-doc-artifacts.ts",
    "docs:contracts:check": "npx tsx scripts/check-doc-contracts.ts",
    "docs:validate": "npm run docs:artifacts:generate && npm run docs:contracts:check && npm run docs:build"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}

{
  "name": "@phdcristian/openedge-dsl",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run -c ./vitest.config.ts",
    "test:watch": "vitest -c ./vitest.config.ts",
    "test:simulator-parity": "node ./scripts/run-simulator-parity.mjs",
    "docs:generate": "npm run docs:generate --workspace @openedge/lang-spec",
    "check:boundaries": "node ./scripts/check-boundaries.mjs"
  },
  "dependencies": {
    "@openedge/compiler-api": "workspace:*",
    "@openedge/compiler-backend-csv": "workspace:*",
    "@openedge/compiler-front": "workspace:*",
    "@openedge/compiler-ir": "workspace:*",
    "@openedge/lang-spec": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.13",
    "typescript": "^5.3.0",
    "vitest": "^1.6.1"
  }
}
